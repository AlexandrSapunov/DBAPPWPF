<Window x:Class="DBAPPWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DBAPPWPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <TabControl>
            <TabItem Header="{DynamicResource Guides}">
                <TabControl>
                    <TabItem Header="{DynamicResource Positions}">
                        
                    </TabItem>
                    <TabItem Header="{DynamicResource Department}">
                        
                    </TabItem>
                </TabControl>
            </TabItem>

            <TabItem Header="{DynamicResource Table}">
                <TabControl>
                    <TabItem Header="{DynamicResource Service}">
                        
                    </TabItem>
                </TabControl>
            </TabItem>
            
        </TabControl>
    </Grid>
</Window>
